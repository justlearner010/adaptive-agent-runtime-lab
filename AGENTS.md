# AGENTS.md — 实验设计决策上下文

> 本文件给所有参与本仓库的协作者（人类与 agent）阅读。**每次设计 / 实现 / 评测 / 改动前，先过一遍本文件。**
> 它回答"我们为什么存在、什么值得做、怎么判断值不值得"。

## 1. 项目最终产出与目的

- **最终产出**：实验报告文档（experiment reports），不是生产系统。
- **最终目的**：搞清楚一个 **router / 调度器** 的设计——让 agent 自主决定"什么时候用哪种执行策略"。
- **远期落点**：**学习式 Policy（v5）**——agent 无需人工干预，自主选择更高效的执行模式。

## 2. 每个实验决策前必须回答的两个问题

> 这是本仓库的最高检查标准。任何改动 / 评测 / 新工具 / 新任务，先问这两句，答不上来就不做（或降级为纯维护）：

1. **这对训练数据有贡献吗？** —— 是否在为"学习式 Policy"提供或改进决策特征与标注数据？
2. **这对实验目的有贡献吗？** —— 是否推进"router 如何选择策略 / 各策略适用范围与边界"的理解？

## 3. 指标优先级（共识）

1. **正确率**——注意是"答对题"不是"格式对"（见 §5）
2. **token 数**（成本）
3. **时间**（延迟）
4. **性价比**（正确 / 成本）

核心目的：**看清楚各策略的适用范围与边界**，不是刷分数。

## 4. 任务粒度（共识）

- 从**简单 prompt** 开始；多轮 / 有状态环境是远期方向，当前不展开，边界未定。
- 评测值必须**确定性、可复现**（本地语料 / 合成文档 / 精确算术），不依赖模型世界观。

## 5. "正确"的定义（共识与演进）

- 旧判定器（关键词 / grounding / 数字匹配）判的是**格式命中**，不是"答对题"。
- v2 引入 **LLM-as-judge** 后，目标判定升级为**答对这个题**（judge 判定）；格式命中只能算弱证据。
- judge 尚未大规模验证——**在 judge 被验证前，报告必须同时标注两种判定的差异**（keyword vs judge 的一致率 / 假阳性 / 假阴性）。

## 6. 过程纪律（现状：未完全做到，必须逐步建立）

- **变量隔离**：一轮实验尽量只动一个变量；改动清单必须在实验报告里显式列出。
  现状：多次"修 bug 与改设计"混在一轮里（如 004 的模型 + 判定器混杂），无法归因——这是已知缺陷。
- **trace 需补过程证据**：现在 trace 只够事后聚合（调用数 / token / 耗时）；回答"subagent 何时超过 react"
  需要**过程数据**（父上下文每步实际 token、worker 报告遗漏、分解粒度、合成是否丢失信息）——v3 必须改 trace。
- **超参需敏感性检查**：`max_steps=8` / `max_steps_per_agent=6` / `workers=2` 均为拍脑袋值。
  结论必须检查对这些超参的敏感性，否则"subagent 超过 react"可能是"react 步数上限太紧"的伪结论。
- **分解粒度需显式化**：planner 自由分解（2 或 4 个 worker）导致运行之间不可比；
  需记录分解粒度并按粒度分组统计，必要时固定分解规则。
- **多模型是待办**：当前单模型（deepseek-v4-flash，推理模型，有独特的空输出 / 高延迟特性）；
  机制结论需多模型验证后才算数。

## 7. 下一轮实验（v3）设计状态

**已定案**：v3 聚焦"SubAgent 在什么维度、什么条件下开始超过 ReAct"。完整设计见 **[v3-design.md](v3-design.md)**。

已解决（2026-08-14，用户确认 + 功效分析）：
- 采样量与统计：N=5 只能分辨 ±6-14pp；"多数票+McNemar"否决；改用每任务正确率 + 配对 t 检验，主矩阵 T=8 × N=20（可检 δ=20pp，功效 95%）
- 任务形态：只做"全读"（检索/选择性留 v4）
- 自变量：文档规模 L1/L2/L3 × 并行结构（独立分块 vs 串行依赖）；"目标类型"不纳入
- 敏感性检查：react max_steps {8,16,32}、worker {1,2,4}，L2 档 × N=5，判据=反超点是否移动超 1 档
- 分解粒度：记录分组（主）+ 固定规则小项

## 8. 决策日志（追加式，最新在上）

| 日期 | 决策 | 依据 / 备注 |
|---|---|---|
| 2026-08-14 | 本周主题：v3「SubAgent 何时超过 ReAct——边界实证」，P0-P4 阶段化执行 | 用户确认 |
| 2026-08-14 | v3 设计定案：只做全读形态 / 做并行结构对照 / 主矩阵 T=8×N=20 / 敏感性按方案 | 用户确认 + 功效分析 |
| 2026-08-14 | 明确最终产出=实验报告文档；最终目的=router/调度器设计 → 学习式 Policy（v5） | 用户确认 |
| 2026-08-14 | 指标优先级：正确率 > token > 时间 > 性价比；目的是适用范围与边界 | 用户确认 |
| 2026-08-14 | "正确"从格式命中升级为 judge 判定（答对题）；judge 验证前双标注 | 用户确认 |
| 2026-08-14 | 任务从简单 prompt 起步；多轮/有状态为远期、边界未定 | 用户确认 |
| 2026-08-14 | v3 聚焦单一问题："SubAgent 在什么维度、什么条件下开始超过 ReAct" | 用户确认 |
| 2026-08-14 | 承认未做变量隔离；trace 需补过程证据；超参/分解粒度需显式化 | 用户自查 |
