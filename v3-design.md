# v3 实验设计：SubAgent 何时超过 ReAct——边界实证

> 状态：设计定案（P1），待实现
> 日期：2026-08-14
> 前置：AGENTS.md（决策上下文）、EXPERIMENT-001..004（背景数据）
> 原则：一次只动一个变量（AGENTS.md §6）；开工前过两个贡献检查

## 1. 研究问题与范围

**主问题**：在长上下文 / 可并行任务上，SubAgent 在**什么维度、什么条件下**开始显著超过 ReAct？

**明确不做**（写入局限）：
- 不做检索定位 / 选择性读取形态（v4 候选）
- 不做"目标类型"维度（枚举 vs 概括，v4 候选）
- 不引入多模型（多模型为单独阶段，本实验固定 deepseek-v4-flash）

**贡献检查**（AGENTS.md §2）：
- 对实验目的：直接产出"长任务委派与否"的边界结论 ✓
- 对训练数据：每任务产出 (任务特征 → 各策略正确率/成本) 标注，供学习式 Policy ✓

## 2. 变量设计

### 2.1 自变量（受控变化）

| 自变量 | 水平 | 说明 |
|---|---|---|
| **文档规模 S** | L1 / L2 / L3 | 2 页~5KB·4 事实 / 4 页~10KB·8 事实 / 8 页~20KB·16 事实（doc 工具多规模扩展） |
| **并行结构 P** | 独立分块 / 串行依赖 | 独立：现状，worker 无依赖；串行：第 1 段给出线索关键词 → 用它定位目标段（强制顺序，subagent 并行优势应消失） |

### 2.2 因变量（每任务 × 每策略）

| 维度 | 度量 | 说明 |
|---|---|---|
| 正确率 | judge 判定（答对题），keyword 双标注 | AGENTS.md §5：judge 验证前必须双标注 |
| token 成本 | 总 tokens + **父上下文 tokens** | 父上下文需 trace 改造（§6） |
| 时间 | wall_ms | 网络波动，报告注明 |
| 机制 | spawns、分解粒度、worker 报告遗漏 | 过程证据（§6） |
| 性价比 | 正确 / tokens（辅助） | AGENTS.md §3 指标优先级 |

### 2.3 控制变量（保持不变）

- 模型 deepseek-v4-flash、max_tokens 设定、ReAct/Subagent 协议、判定器逻辑、doc 文档内容模板（仅规模变化）

## 3. 任务设计

- **独立分块**：沿用 v2 longdoc 任务（"读文档 X 报全部事实"），按规模换文档
- **串行依赖**（新任务类）：两阶段——"读文档 X 的第 1 段，找到线索关键词 K；再用 K 定位并读取目标段，报告 K 描述的对象"。判定 = 目标事实关键词
- 每 (S × P) 格 **T=8 任务**（同一模板 × 4 文档 × 2 措辞，或按规模生成足够事实池）

## 4. 采样与统计（功效分析支撑）

### 4.1 现状（真实 Phase 3 数据，块 bootstrap 95% CI）

- 合并 8 任务后 cell 级 CI 宽度 ±6~14pp；N=5 只能分辨巨大差异
- **"多数票 + McNemar"设计被否决**：T≤16、N≤20 全网格功效 <30%（多数票压掉连续信息）

### 4.2 统计方案

- **主检验**：每任务正确率之差 d_t = rate_sub(t) − rate_react(t)，对 T 个任务做**配对 t 检验**（两尾 α=0.05）；同时报告 d_t 的 bootstrap 95% CI
- **主矩阵采样**：**T=8 × N=20 / 策略** → 可检 δ=20pp，功效 95%（模拟）
- 若预算紧张：T=12 × N=10（功效 90%）或 T=16 × N=10（97%）为备选
- 拐点判据：对每个 (S, P) 格做配对检验；subagent 显著且 CI 下限 >0 的**最低规模档**记为"开始超过"点

## 5. 敏感性检查（Q6，防伪结论）

- react `max_steps ∈ {8(现状), 16, 32}`，只跑 **L2 × 独立** 格 × N=5
- worker 数 `∈ {1(串行), 2(现状), 4}`，同上
- **判据**：subagent 反超点（按 §4.2 定义）随超参变化是否移动超过 1 个规模档；移动则结论标记为"超参依赖"，不写入机制结论

## 6. trace 改造（过程证据，P2 实现）

- 记录每次 llm_call 时**父上下文序列化长度**（近似 token）→ 支撑"父上下文膨胀"度量
- 记录 subagent **分解粒度**（spawns 已有）与**每 worker 报告**（事实命中数/遗漏数，用 doc 事实表对照）
- 记录合成环节是否丢失信息（worker 报告事实数 vs 最终答案事实数）

## 7. 分解粒度控制（Q7）

- **主方案**：记录分组（零侵入）——每任务记 spawns，报告按 spawns 分组统计
- **小项**：固定规则对照——planner 输出按"每 2500 字符一段"重组（或 prompt 强制"每页一个 worker"），T=4 × N=5

## 8. 实验矩阵与预算

| 模块 | 范围 | 执行次数（估算） |
|---|---|---|
| 主矩阵 | 3S × 2P × T8 × N20 × 2 策略 | 1920 |
| direct 对照 | 同上 × direct（验证 0% 基线，N=5 即可） | 480 |
| 敏感性 | max_steps×3 + worker×3，L2×独立，N5×2 策略 | 60 |
| 粒度小项 | T4 × N5 × 2 策略 | 40 |

合计 ≈ **2500 次执行**，预估调用 2-3 万次 / ¥100±50（以 pilot 实测校准）。
**Pilot 先行**：新任务（L1/L3、串行依赖）先 N=5 冒烟，校准单次成本与判定可行性，再决定主矩阵规模。

## 9. 验收（Definition of Done）

1. EXPERIMENT-005 回答：在哪个 (S, P) 格、哪个维度（正确率/父上下文 token/时间/总成本）subagent 显著超过 react；哪些维度不成立
2. 关键结论通过 §5 敏感性检查（或显式标注超参依赖）
3. 配对检验 p 值 + bootstrap CI 齐全；keyword 与 judge 双标注差异列明
4. trace 过程证据落地（§6）
5. 决策日志更新（AGENTS.md §8）

## 10. 风险与开放项

- 串行依赖任务可能过难/过易 → pilot 校准
- 预算超支 → 按 §4.2 备选配置降档
- 推理模型延迟高 → 全量耗时预估 3-6 小时，分批运行
